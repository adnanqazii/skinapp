import {
    StyleSheet,
    TextInput,
    View,
    Text,
    Image,
    KeyboardAvoidingView,
    Keyboard,
    TouchableOpacity,
    ScrollView,
    SafeAreaView,
    Button
  } from 'react-native';
const Home = ({ navigation }) => {
    return (
     <View>
      <Text>THIS IS HOME PAGE</Text>

     </View>
    );
  };

export default Home;